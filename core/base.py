class Pathfinder:
    def __init__(self, graph, nodes):
        self.graph = graph
        self.nodes = nodes

    def find_path(self, start, end, hour, mode):
        raise NotImplementedError("Bạn phải cài đặt hàm find_path trong class con")